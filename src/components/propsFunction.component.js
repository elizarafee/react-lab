import React from 'react'

const Pro = ({fname, sname}) => {
    // console.log(props)
    return (
        <div>
            <h1>first name : {fname}, second name : {sname}</h1>
        </div>
    )
}

export default Pro; 