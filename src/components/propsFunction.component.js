import React from 'react'

const Pro = props => {
    const {fname, sname} = props
    return (
        <div>
            <h1>first name : {fname}, second name : {sname}</h1>
        </div>
    )
}

export default Pro; 